/*import { storiesOf } from '@storybook/html';
import { Button } from './button.stories';
import './addon-actions.stories';

export const withText = () => '<button class="btn">Hello World</button>';

const withEmoji = () => {
  const button = document.createElement('button');
  button.innerText = '😀 😎 👍 💯';
  return button;
};

storiesOf('Test', module)
  .add('example', () => withText());

storiesOf('Test 1', module)
  .add('example 1', () => withEmoji());

storiesOf('Test 2', module)
  .add('example 2', () => Button());
*/
